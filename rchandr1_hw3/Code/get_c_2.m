function c = get_c_2(dels, K)
c{1} = 1./(1 + (dels{1}/K).^2);
c{2} = 1./(1 + (dels{2}/K).^2);
c{3} = 1./(1 + (dels{3}/K).^2);
c{4} = 1./(1 + (dels{4}/K).^2);
c{5} = (1./(1 + (dels{5}/K).^2))/sqrt(2);
c{6} = (1./(1 + (dels{6}/K).^2))/sqrt(2);
c{8} = (1./(1 + (dels{8}/K).^2))/sqrt(2);
c{7} = (1./(1 + (dels{7}/K).^2))/sqrt(2);

end